name: pipeline
type: dataflows
steps:
  - step: load
    loadSource: 'capital.csv'
  - step: set-type
    name: id
    type: string
  - step: dump-to-path
    outPath: 'output'
    prettyDescriptor: true
